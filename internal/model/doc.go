// Package model defines the shared data types used across all pipeline services.
//
// The central type is TelemetryMessage, which maps directly to the CSV columns
// and is the wire format for messages flowing through the queue.
package model
